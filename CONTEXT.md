# CONTEXT — 용어집

이 저장소의 측정과 기록에 쓰는 말들. 여기 정의가 애매해지면 지표 전체가 흔들리므로,
용어를 바꿀 때는 이 파일을 먼저 고친다.

## Factory (공장)

앱을 만드는 반복 가능한 절차 전체: 스펙 인터뷰 → SPEC.md 승인 → 새 세션 one-shot 실행
→ 개입 기록 → 주간 로그 → 다음 스펙에 반영. v0은 코드가 아니라 템플릿과 규칙의 묶음이다.

## one-shot

승인된 SPEC.md를 **새 세션**에 투입한 순간부터 AI가 완료를 선언할 때까지의 자율 실행 1회.
스펙 인터뷰와 스펙 작성은 one-shot에 포함하지 않는다(공장의 입력 제작 단계). 단, 스펙
제작에 걸린 시간은 별도로 기록한다.

## intervention (개입)

one-shot 완료 선언 이후, 사람이 개입해 **해결한 문제 1건 = 1회**. 메시지 수가 아니라
문제 단위로 센다. 종류는 다섯 가지:

| 종류 | 뜻 |
|---|---|
| 설계 오류 | 스펙 해석이 잘못됐거나 구조 자체를 다시 잡아야 했음 |
| 구현 버그 | 동작이 틀려서 고침 |
| 스타일 | 동작은 맞지만 시각/코드 스타일을 고침 |
| 누락 | 스펙에 있는데 만들어지지 않음 |
| 진행 넛지 | "계속해", "테스트 돌려봐" 등 실행을 밀어준 것 |

진행 넛지는 앱의 문제가 아니라 공장(실행 환경)의 문제 신호이므로, 집계와 그래프에서
나머지 넷과 분리한다.

## first-pass

one-shot 완료 선언 직후, 스펙의 acceptance criteria를 사람이 체크해 전부 통과하면
first-pass 성공. (W4부터 채점판으로 자동화 예정)

## 장면 (scene)

앱을 한 문장으로 그릴 수 있는 화면: "누가, 무엇을 하는 화면인가". 장면이 안 그려지는
앱은 라인업에 넣지 않는다. 장면의 주인공은 소비자일 수도, 개발자 자신일 수도 있다.

## 격차 (gap)

이 앱이 채우려는 학습 항목 하나 (예: structured output, RAG, RSC). 앱 하나에 격차
하나 — 여러 개를 물리면 장면이 죽는다.

## golden set

채점판(W4)에서 쓰는, 정답을 사람이 미리 확정해 둔 입력·출력 쌍의 묶음. 모델이나
프롬프트가 바뀔 때마다 이 묶음으로 회귀 채점한다.
