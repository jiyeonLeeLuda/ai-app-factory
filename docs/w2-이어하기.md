# W2 이어하기 — 다음 세션 진입 (핸드오프)

> 사용법: **새 세션**을 저장소 루트에서 열고 "이 파일 읽고 이어서 진행"이라고 투입.
> 이 세션 목표: W2(AI 음성통화) **pre-spec을 마저 끝내고 SPEC.md까지 작성**. (원샷 실행은 그 다음 새 세션)
> 작성 2026-09-16 (W2 방식 심의 + pre-spec 착수 세션 종료 시점).

## 0. 먼저 읽을 것 (컨텍스트 복원)

- **팩토리 공정 결정**: `docs/회고/2026-09-16-팩토리-스펙작성방식-결정.md` — 방식(감산형 pre-spec 게이트)·깃털 0단계·deep-interview 효용성 판정
- **W2 씨앗 (제품 초안 + 선행조사 + 결정)**: `docs/w2-prespec-씨앗.md` ← **주 재료**
- 3축 프레임 메모리(`three-axis-job-change-frame`), `docs/p1-spec.md`(W1 확정), `factory/SPEC_TEMPLATE.md`, `docs/스펙-작성-체크리스트.md`

## 1. 지금까지 확정된 것 (재론 금지 — 씨앗에 근거)

- 격차 = **음성 왕복 STT→LLM→TTS**, 전송 **WebSocket**, 벤더 **OpenAI discrete**(Realtime 통짜 블랙박스 아님)
- 구현 경로 = **A안 Chained**: 브라우저 mic → ws → Next.js 서버 릴레이 → `gpt-4o-mini-transcribe`(STT, HTTP) → LLM → `gpt-4o-mini-tts`(TTS, HTTP) → ws → 브라우저
- **Step 0**(씨앗 §0): 목적 + 축 tilt(Y1 > X2[비교문서로 이번주 실함] > Z3) + 축별 승리조건
- **auth 미룸** — W2는 음성 단일 격차. auth는 W4(개인화) 직전 전용 슬롯
- 통화 **휘발**(DB 미저장) · 첫 턴 **assistant**("여보세요/hello") · `max_output_tokens` **1000**
- 라인업: W2.5(얼굴트래킹 WebRTC) / W2.9(RN 포팅) / W3(AI 영상통화 = 도전과제) — README

## 2. 다음 한 스텝 (이 세션 할 일)

씨앗 **§7의 남은 🟡**을 채워 SPEC 재료를 완성 → `SPEC_TEMPLATE.md`로 **W2 SPEC.md** 작성:

- **S3 음성 엣지케이스**: STT 빈결과·침묵 타임아웃, mic 권한 거부, 통화 중 새로고침·네트워크 끊김, TTS 실패
- **S4 나머지 제약값**: 언어(ko/en 혼용), TTS voice(필립용), 오디오 포맷(입력 WebM/Opus·출력 mp3), 의존성 핀(OpenAI SDK·ws 라이브러리), **Realtime API 의도적 미사용** 명시, Vercel WS 5분 제약
- **S2**: 통화 휘발(Message 미저장) 반영, 첫 턴 assistant
- **Out of Scope**: 립싱크·영상(W3), 진짜 양방향 barge-in(W2.5/W3), auth(W4 전)
- **사람 준비물**: 새 prereq 없음(`OPENAI_API_KEY` 재사용) 확인
- 선행조사(씨앗 §3·§6 링크)를 SPEC S4에 **구체값**으로 반영

## 3. 실측/열린 항목 (Acceptance 또는 조사)

- **턴제(VAD 침묵감지) 자연스러움** — 원샷 후 사람 체크(run1 "타이핑≠말풍선" 교훈)
- **auth 슬롯 위치** 미정(W4 전 어딘가)

## 4. 가드레일 (이 세션 교훈)

- 자를 때 **3축으로 채점**(vibes 금지), **격차 하나** 유지(감산)
- API 규격·모델명은 **공식문서**에서(씨앗 §6 링크), 기억으로 쓰지 말 것
- 기록 분리: **회고 = 공정(팩토리) / 씨앗·스펙 = 제품(W2)** — 섞지 말 것(측정선 오염 금지)
